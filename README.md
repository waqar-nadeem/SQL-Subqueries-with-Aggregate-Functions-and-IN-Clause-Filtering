# SQL-Subqueries-with-Aggregate-Functions-and-IN-Clause-Filtering
This query demonstrates the use of SQL subqueries in Oracle to filter data based on derived results. It covers scalar subqueries with aggregate functions (AVG) and multi row subqueries using the IN clause to retrieve employees by salary comparison and department location criteria.
